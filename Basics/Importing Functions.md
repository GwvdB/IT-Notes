In many languages, you can import functions from external libraries or modules. This allows you to use pre-built functionality without rewriting it.

### Example

python
```
import math
print(math.sqrt(25))
```